import pyxel as px

#Tela de inicio/Welcome

class Home:
	def __init__(self):
		
	def update(self):
		if px.btnp(px.KEY_Q):
			px.quit()
		elif px.btnp(px.KEY_SPACE) or px.btnp(px.KEY_RETURN):
			frame.jogo_start() #variável pertencente a frames que será declarada futuramente
		elif mouse_x == 

	def draw(self):
		pass